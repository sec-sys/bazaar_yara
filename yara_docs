Бестиарий хостовика - статья созданная для общего пользования всем специалистам по исследованиям киберугроз, которые занимаются покрытием хостовой активности (они же yara-правила). Словарь содержит в себе различные строки, которые могут попадаться при статическом анализе.
Система оценивания строк:
    (0) -- Легитимная строка
    (1) -- Повсеместно встречающаяся строка
    (2) -- Зловредная строка
    (-) -- Информация отсутствует
Библиотеки
mscoree.dll (0) - расширение браузера IE. Файл библиотеки Microsoft, который необходим для выполнения приложений на «управляемом коде», т. е. приложений, написанных для использования совместно с Microsoft .NET Framework. Он, таким образом, является частью Microsoft .NET Framework.
comctl32.dll (0) - отвечает за отображение графических элементов. Динамическая библиотека, которая отвечает за специальный компонент системного пакета библиотек Общих Элементов Управления (Common Controls Library), которая появился в Windows ещё с приходом Internet Explorer 4.0.
comdlg32.dll (0) - отвечает за визуальные эффекты. Относится к основным системным файлам ОС Windows. Обычно он содержит набор процедур и функций драйвера, которые могут применяться Windows. Comdlg32.dll file, также известен как Common Dialogs DLL, обычно ассоциируется с Microsoft® Windows® Operating System. Это важный компонент, обеспечивающий правильную работу программ Windows.
Ole32.dll (0) - представляет собой файл библиотеки динамических ссылок (DLL) в операционной системе Microsoft Windows. Это расшифровывается как 32-разрядная библиотека связывания и встраивания объектов (OLE). OLE - это технология корпорации Майкрософт, которая позволяет приложениям создавать составные документы, содержащие несколько типов данных, и манипулировать ими.
Kernel32.dll (0) - динамически подключаемая библиотека, являющаяся ядром всех версий ОС Microsoft Windows. Она предоставляет приложениям многие базовые API Win32, такие как управление памятью, операции ввода-вывода, создание процессов, потоков и функции синхронизации.
Ntdll.dll (0) - это ключевой компонент Windows NT/2000/XP/2003. NTDLL является прослойкой между стандартом Win32 API (функции типа CreateFile, PostMessage и т.д.) и NT Native API (функции типа NtCreateFile, KeCreateSemaphore и им подобные). В Windows 9x библиотеки Win32 (KERNEL32.DLL, USER32.DLL, GDI32.DLL, ...) сами вызывали прерывания для передачи управления в ядро (VMM), а в системах на ядре NT они прилинкованы к NTDLL.DLL и та уже вызывает прерывания для обращения к ядру NT.
winspool.dll (0) - драйвер для взаимодействия с принтером.
User32.dll (0) - является основной библиотекой DLL Windows, используемой для управления окнами и других интерактивных задач пользовательских программ. WPF использует ее нечасто. Windows.Forms - это в основном .net-оболочка для User32, и поэтому она по-прежнему полностью основана на GDI, GDI + и оконных дескрипторах.
Shlwapi.dll (0) - это файл, используемый Windows для загрузки настроек цвета и других важных функций графического интерфейса пользователя системы Windows.
Gdi32.dll (0) - 32-разрядный интерфейс графического устройства. Он содержит функции, необходимые интерфейсу графического устройства Windows или GDI для печати графики и текста на дисплее и принтере.
oleaut32.dll (0) - Библиотека представляет собой системный компонент, который отвечает за работу с оперативной памятью.
Winmm.dll (0) - (также известный как Windows Multimedia API) — это файл динамической библиотеки ссылок, который является неотъемлемой частью операционной системы Windows. Он содержит функции и ресурсы, которые позволяют приложениям взаимодействовать с мультимедийными устройствами и сервисами. Winmm.dll обеспечивает поддержку различных мультимедийных операций, включая воспроизведение звука, последовательность MIDI, ввод с джойстика и функции таймера. Он выступает в роли моста между операционной системой и мультимедийными приложениями, позволяя им эффективно использовать доступные аппаратные и программные ресурсы.
Userenv.dll (0) - отвечает за управление параметрами пользовательской среды, включая профили пользователей, групповые политики и параметры реестра. Этот файл обеспечивает применение соответствующих настроек при входе пользователя в учетную запись Windows.
Uxtheme.dll (0) - отвечает за работу с визуальными стилями и темами в Windows, позволяя пользователям настраивать внешний вид рабочего стола. Этот файл в основном используется для включения сторонних тем и визуальных стилей, которые не поддерживаются Windows.
imagehlp.dll (0) - это модуль, содержащий библиотеку Windows Debug Help, используемую для отладки и обработки ошибок в операционных системах Windows NT и более поздних версиях.
authz.dll (0) - это модуль авторизации, связанный с операционной системой Microsoft® Windows® от Microsoft Corporation.
Oledlg.dll (0) — это библиотека которая содержит базовые OLE функции, которые нужны для реализации диалоговых окон в Windows с GUI. Этот файл нужен для того, чтобы превратить Server Core в настоящую рабочую станцию.
MSIMG32.dll (0) - является частью Windows и предоставляет функции для работы с графикой, особенно для обработки изображений и работы с прозрачностью. Она используется в основном для создания и манипуляции графическими элементами в приложениях.
shell32.dll (1) - Cодержит широкий спектр функций и ресурсов, связанных с оболочкой Windows, включая операции с файлами и папками, управление рабочим столом, функции панели задач, значки, контекстные меню и расширения оболочки. (есть вредоносы маскирующиеся под него).
wininet.dll (1) - для взаимодействия с FTP\HTTP. Файл, содержащий набор функций, которые позволяют приложениям Windows взаимодействовать через Интернет. Вы можете найти файл в следующем расположении: C:\Windows\System32. Он предназначен в первую очередь для использования Microsoft Internet Explorer, но его также могут использовать другие приложения, которым требуется доступ к типам данных, обычно находящимся в Интернете.
WS2_32.dll (1) - отвечает за открытие сокетов. Ws2_32.dll загружает библиотеку DLL интерфейса поставщика услуг в систему с помощью стандартных механизмов загрузки динамической библиотеки Microsoft Windows и инициализирует ее путем вызова WSPStartup. Обычно это активируется приложением, вызывающим сокет или WSASocket для создания нового сокета, связанного с поставщиком услуг, библиотека DLL интерфейса которого в настоящее время не загружается в память.
msvbvm60.dll (1) - DLL-файлы, такие как msvbvm60.dll, по сути являются справочником, хранящим информацию и инструкции для исполняемых файлов (EXE-файлов), например MpSigStub.exe. Данные файлы были созданы для того, чтобы различные программы (например, Easy PDF Converter) имели общий доступ к файлу msvbvm60.dll для более эффективного распределения памяти, что в свою очередь способствует повышению быстродействия компьютера.
Vba6.dll (1) - файл DLL (Dynamic Link Library): разработанный_, который относится к основным системным файлам ОС Windows. Обычно он содержит набор процедур и функций драйвера, которые могут применяться Windows. Vba6.dll file, также известен как Visual Basic for Applications Development Environment, обычно ассоциируется с Visual Basic for Applications Development Environment.
hostfxr.dll (1)- это файл, используемый в дистрибутиве Visual C++ Redistributable для Visual Studio 2015.
Advapi32.dll (1) - является частью библиотеки advanced API services library. Предоставляет доступ к расширенной функциональности, которая поставляется в дополнение к ядру. Он отвечает за такие вещи, как реестр Windows, перезапуск и завершение работы системы, запуск / остановка и создание служб Windows, а также за управление учетными записями пользователей.
Riched32.dll (1) - файл DLL (Dynamic Link Library): разработанный_, который относится к основным системным файлам ОС Windows. Обычно он содержит набор процедур и функций драйвера, которые могут применяться Windows. Riched32.dll file, также известен как Wrapper Dll for Richedit 1.0, обычно ассоциируется с Microsoft® Windows® Operating System.
Wsock32.dll (1) - Это динамическая системная библиотека API созданная Microsoft, которая содержит в себе набор инструкций сетевых функций. Другими словами, если файла wsock32.dll не будет в системе или он поврежден, то пользователь будет испытывать проблемы с сетью, интернетом и играми и программами, которые работают через сеть или интернет.
version.dll (1) - считается разновидностью DLL-файла. DLL-файлы, такие как version.dll, по сути являются справочником, хранящим информацию и инструкции для исполняемых файлов (EXE-файлов), например sapisvr.exe. Данные файлы были созданы для того, чтобы различные программы (например, Windows) имели общий доступ к файлу version.dll для более эффективного распределения памяти, что в свою очередь способствует повышению быстродействия компьютера.
MPR.dll (1) - Библиотека маршрутизации для нескольких служб доступа.
Psapi.dll (1) - представляет собой файл библиотеки динамических ссылок в операционной системе Microsoft Windows, который предоставляет API для извлечения информации о процессах и модулях, запущенных в системе. Разработчики программного обеспечения и системные администраторы обычно используют его для мониторинга и анализа процессов и модулей в средах Windows.
Iphlpapi.dll (1) - это один из видов DLL-файлов, связанный с интерфейсом прикладного программирования IP helper. Этот DLL-файл отвечает за функции и службы, связанные с сетью.
msvcrt.dll (1) - программный документ в операционной системе Windows, который отвечает за взаимодействие с программами, написанными на языке программирования С++.
LibVLC.dll (1) - является файлом библиотеки динамических ссылок, входящим в состав программного обеспечения VLC Media Player. Он содержит набор функций и процедур, которые обеспечивают программный доступ к движку VLC Media Player engine. Библиотека позволяет разработчикам встраивать функциональность VLC Media Player в свои приложения или использовать VLC как автономный проигрыватель.
Bcrypt.dll (1) - это файл операционной системы Microsoft Windows, называемый библиотекой криптографических примитивов Windows. Он находится в папке System32.
Vcruntime140.DLL (1) – библиотека, которая входит в состав Microsoft Visual C++, данный файл является важным компонентом операционных систем Windows 7 и Windows 10.
Crypt32.dll (1) - это модуль, реализующий многие функции обмена сертификатами и криптографическими сообщениями в CryptoAPI, такие как CryptSignMessage. Crypt32.dll — это модуль, который поставляется с операционными системами Windows и Windows Server, но разные версии этой библиотеки DLL предоставляют различные возможности.
mshtml.dll (1) - это модуль, содержащий служебные функции, связанные с HTML.
odbc32.dll (1) - содержит функции для стандарта запросов к базам данных ODBC.
activeds.dll (1) - это модуль, содержащий функции и методы объектов, или COM-компоненты, для API Active Directory Services Interfaces (ADSI).
setupapi.dll (1) - это библиотека, которую используют программы установки и настройки.
WTSAPI32.dll (1) - интерфейсы для работы с удаленными рабочими столами и службами терминалов. Она позволяет разработчикам управлять сессиями удаленного рабочего стола, получать информацию о пользователях и сессиях, а также выполнять различные действия, связанные с удаленными подключениями.
IPHLPAPI.DLL (1) - (Internet Protocol Helper API) предоставляет функции для работы с сетевыми интерфейсами и протоколами в операционных системах Windows. Позволяет разработчикам получать информацию о сетевых настройках и управлять сетевыми соединениями.
WINHTTP.dll (1) - предоставляет интерфейсы для работы с HTTP и HTTPS в операционных системах Windows. Она предназначена для приложений, которые требуют взаимодействия с веб-сервисами и интернет-ресурсами.
Sysconfig.dll (2) - Библиотека непонятного происхождения
DropCreateDatabaseAlways — это тип инициализации базы данных, при котором она удаляется и создаётся заново при каждом запуске приложения, независимо от того, изменилась ли модель данных. Данный тип инициализации используется крайне редко.
System.IO — это пространство имён в .NET, которое посвящено службам файлового ввода-вывода и ввода-вывода из памяти.
В этом пространстве определён набор классов, интерфейсов, перечислений, структур и делегатов.
Основные классы из пространства имён System.IO:
    — BinaryReader, BinaryWriter;
    — BufferedStream;
    — Directory, DirectoryInfo;
    — DriveInfo;
    — File, FileInfo;
    — FileStream;
    — MemoryStream;
    — Path;
    — StreamReader, StreamWriter;
    — StringReader, StringWriter.
